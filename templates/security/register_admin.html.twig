{% extends 'base.html.twig' %}

{% block title %}Créer un compte administrateur{% endblock %}

{% block body %}
<div class="fr-container fr-py-6w">
    <div class="fr-grid-row fr-grid-row--center">
        <div class="fr-col-12 fr-col-md-8 fr-col-lg-6">
            <h1 id="page-title" class="fr-h1 fr-mb-4w">Créer un compte administrateur</h1>

            <div class="fr-alert fr-alert--warning fr-mb-4w" role="status">
                <h3 class="fr-alert__title">Attention</h3>
                <p>Vous êtes sur le point de créer un compte administrateur avec des privilèges étendus. Assurez-vous de choisir un mot de passe sécurisé.</p>
            </div>

            {{ form_start(registrationForm, {'attr': {'aria-labelledby': 'page-title', 'novalidate': 'novalidate'}}) }}
                <div class="fr-input-group fr-mb-4w">
                    {{ form_label(registrationForm.identifiant, null, {'label_attr': {'class': 'fr-label'}}) }}
                    {{ form_widget(registrationForm.identifiant) }}
                    {{ form_errors(registrationForm.identifiant) }}
                </div>

                <div class="fr-input-group fr-mb-4w">
                    {{ form_label(registrationForm.plainPassword.first, null, {'label_attr': {'class': 'fr-label'}}) }}
                    {{ form_widget(registrationForm.plainPassword.first) }}
                    {{ form_errors(registrationForm.plainPassword.first) }}
                </div>

                <div class="fr-input-group fr-mb-4w">
                    {{ form_label(registrationForm.plainPassword.second, null, {'label_attr': {'class': 'fr-label'}}) }}
                    {{ form_widget(registrationForm.plainPassword.second) }}
                    {{ form_errors(registrationForm.plainPassword.second) }}
                </div>

                <div class="fr-callout fr-callout--purple-glycine fr-mb-4w">
                    <h3 class="fr-callout__title">Rôle : Administrateur</h3>
                    <p class="fr-callout__text">
                        Ce compte aura accès à :
                    </p>
                    <ul>
                        <li>Tableau de bord administrateur</li>
                        <li>Gestion des inscriptions (validation/refus)</li>
                        <li>Gestion des utilisateurs</li>
                        <li>Export des données</li>
                    </ul>
                </div>

                <div class="fr-btns-group">
                    <button class="fr-btn" type="submit" title="Valider et créer le compte administrateur">
                        Créer le compte administrateur
                    </button>
                    <a class="fr-btn fr-btn--secondary" href="{{ path('app_home') }}" title="Annuler et retourner à l'accueil">
                        Annuler
                    </a>
                </div>
            {{ form_end(registrationForm) }}
        </div>
    </div>
</div>
{% endblock %}
